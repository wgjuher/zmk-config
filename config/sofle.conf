# Включить OLED дисплей и Bluetooth только на левой половине
if CONFIG_ZMK_SPLIT_LEFT
    CONFIG_ZMK_DISPLAY=y
    CONFIG_BT=y
    CONFIG_BT_CENTRAL=y
    CONFIG_BT_PERIPHERAL=y

    CONFIG_BT_DEVICE_NAME="Sofle Left v2"
    CONFIG_BT_CTLR_CONN_INTERVAL_MIN=25
    CONFIG_BT_CTLR_CONN_INTERVAL_MAX=50
    CONFIG_BT_CTLR_MIN_LATENCY=0
    CONFIG_BT_CTLR_CONN_SUPERVISION_TIMEOUT=500
endif

# Правая половина без дисплея и Bluetooth
if CONFIG_ZMK_SPLIT_RIGHT
    # Правая половина обычно проще — без дисплея и BT
    # Можно добавить другие настройки, если нужны
endif
